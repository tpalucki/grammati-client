import {SubscriptionData} from './subscription-data';

describe('SubscriptionData', () => {
    it('should create an instance', () => {
        expect(new SubscriptionData()).toBeTruthy();
    });
});
